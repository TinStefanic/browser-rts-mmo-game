@page "{id:int?}"
@using BrowserGame.Internal
@using BrowserGame.Models
@using BrowserGame.Static
@model BrowserGame.Pages.Game.OuterCityModel

@{
    var cityManager = (CityManager)ViewData["CityManager"];
}

<partial name="./Shared/_CommonGameNavigation" view-data="ViewData" />

<div class="row text-center justify-content-center">
    <div class="col-md-3">
        <div class="row justify-content-center">
            <div class="col-8 text-@HtmlColors.Clay">
                <div><h3>Clay</h3></div>
                <div class="row justify-content-around">
                    @foreach (ResourceField resourceField in cityManager.Clay.Fields) {
                        <div class="col-5 mt-2 border border-@HtmlColors.Clay">
                            <a class="text-@HtmlColors.Clay" asp-area="" asp-page="/Game/UpgradeField" asp-route-id="@resourceField.Id">
                                <h4>@resourceField.Level</h4>
                            </a>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-3">
        <div class="row justify-content-center">
            <div class="col-8 text-@HtmlColors.Iron">
                <div><h3>Iron</h3></div>
                <div class="row justify-content-around">
                    @foreach (ResourceField resourceField in cityManager.Iron.Fields) {
                        <div class="col-5 mt-2 border border-@HtmlColors.Iron">
                            <a class="text-@HtmlColors.Iron" asp-area="" asp-page="/Game/UpgradeField" asp-route-id="@resourceField.Id">
                                <h4>@resourceField.Level</h4>
                            </a>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="row justify-content-center">
            <div class="col-8 text-@HtmlColors.Wood">
                <div><h3>Wood</h3></div>
                <div class="row justify-content-around">
                    @foreach (ResourceField resourceField in cityManager.Wood.Fields) {
                        <div class="col-5 mt-2 border border-@HtmlColors.Wood">
                            <a class="text-@HtmlColors.Wood" asp-area="" asp-page="/Game/UpgradeField" asp-route-id="@resourceField.Id">
                                <h4>@resourceField.Level</h4>
                            </a>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-9">
        <div class="row justify-content-center">
            <div class="col-8 text-@HtmlColors.Crop">
                <div><h3>Crop</h3></div>
                <div class="row justify-content-around">
                    @foreach (ResourceField resourceField in cityManager.Crop.Fields) {
                        <div class="col-2 mt-2 mr-2 border border-@HtmlColors.Crop">
                            <a class="text-@HtmlColors.Crop" asp-area="" asp-page="/Game/UpgradeField" asp-route-id="@resourceField.Id">
                                <h4>@resourceField.Level</h4>
                            </a>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<partial name="./Shared/_CommonBuildProgress" view-data="ViewData"/>
